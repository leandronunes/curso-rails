<% div_for comment do %>
  <p>
    <strong> Created at <%= time_ago_in_words(comment.created_at) %> </strong> <%= link_to_remote 'Delete', :url => post_comment_path(@post, comment), :method => :delete %><br/>
    <%= h(comment.body)%>
  </p>
<% end %>
